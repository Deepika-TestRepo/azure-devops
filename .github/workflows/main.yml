# Test file to create pipeline

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]  

jobs:
  build:
    runs-on: ubuntu-latest
# run the file
    steps:
    - uses: actions/checkout@v2     
    - name: Run Make Commands
      run: make hello
